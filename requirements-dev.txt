# Development dependencies for COSMOS 0.1 CI and local quality checks.
# Core runtime modules currently rely primarily on the Python standard library.

pytest>=8.0,<9.0
ruff>=0.6,<1.0
mypy>=1.10,<2.0
